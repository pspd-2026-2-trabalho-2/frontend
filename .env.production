# Keycloak: same-origin em produção (app servido em kiriland.unb.br), sem CORS
VITE_KEYCLOAK_URL=/keycloak
VITE_KEYCLOAK_REALM=grupo03
VITE_KEYCLOAK_CLIENT_ID=pseudopep-frontend

# API Gateway via ingress do cluster
VITE_API_URL=/grupo3
